---
---
<script is:inline>
  (function() {
    const getInitialTheme = () => {
      if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
        return localStorage.getItem('theme');
      }
      return 'modern';
    };

    const getInitialMode = () => {
      if (typeof localStorage !== 'undefined' && localStorage.getItem('mode')) {
        return localStorage.getItem('mode');
      }
      if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
        return 'dark';
      }
      return 'light';
    };

    const getInitialCosmic = () => {
      if (typeof localStorage !== 'undefined' && localStorage.getItem('cosmic')) {
        return localStorage.getItem('cosmic');
      }
      return 'none';
    };

    const theme = getInitialTheme();
    const mode = getInitialMode();
    const cosmic = getInitialCosmic();

    document.documentElement.dataset.theme = theme;
    document.documentElement.dataset.cosmic = cosmic;

    if (mode === 'dark') {
      document.documentElement.classList.add('dark');
      document.documentElement.style.colorScheme = 'dark';
    } else {
      document.documentElement.classList.remove('dark');
      document.documentElement.style.colorScheme = 'light';
    }

    window.__switchMode = () => {
      const isDark = document.documentElement.classList.contains('dark');
      const newMode = isDark ? 'light' : 'dark';

      if (newMode === 'dark') {
        document.documentElement.classList.add('dark');
        document.documentElement.style.colorScheme = 'dark';
      } else {
        document.documentElement.classList.remove('dark');
        document.documentElement.style.colorScheme = 'light';
      }

      localStorage.setItem('mode', newMode);
    };

    window.__switchTheme = (newTheme) => {
      document.documentElement.dataset.theme = newTheme;
      localStorage.setItem('theme', newTheme);
    };

    window.__switchCosmic = (newVariant) => {
      document.documentElement.dataset.cosmic = newVariant;
      localStorage.setItem('cosmic', newVariant);
    };
  })();
</script>
