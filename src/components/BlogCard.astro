---
interface Props {
  title: string;
  description: string;
  url: string;
  pubDate: string;
}

const { title, description, url, pubDate } = Astro.props;
const formattedPubDate = new Date(pubDate);
---

<article class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow">
  <a href={url} class="block p-6">
    <h2 class="text-xl font-bold mb-2 text-gray-900 dark:text-gray-100">{title}</h2>
    <p class="text-gray-600 dark:text-gray-300 mb-4">    
      {description}
    </p>
    <time datetime={formattedPubDate.toISOString()} class="text-sm text-gray-500 dark:text-gray-400">
      {formattedPubDate.toLocaleDateString(undefined, { year: 'numeric', month: 'long', day: 'numeric' })}
    </time>
  </a>
</article>
